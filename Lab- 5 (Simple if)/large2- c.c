#include<stdio.h>
void main ()
{
	int a,b;
	printf("enter the value of a");
	scanf("%d",&a);

	printf("enter the value of b");
	scanf("%d",&b);

	if(a>b)
	{
		printf("%d is larger than %d",a,b);
	}

	if(a<b)
	{

		printf("%d is larger than %d",a,b);
	}
}